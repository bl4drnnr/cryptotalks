{
  "name": "cryptotalks",
  "version": "0.0.1",
  "description": "Cryptotalks - forum/blog for cryptogeeks.",
  "scripts": {
    "dev:build": "docker-compose --env-file .env.development up --build",
    "dev:up": "docker-compose --env-file .env.development up",
    "prod:build": "docker-compose --env-file .env.production up --build",
    "prod:up": "docker-compose --env-file .env.production up",
    "down:dev": "docker-compose --env-file .env.development down",
    "down:prod": "docker-compose --env-file .env.production down",
    "front:dev": "NODE_ENV=development npm run dev --workspace=front",
    "front:build": "npm run build --workspace=front",
    "api:dev": "NODE_ENV=development npm run start:dev --workspace=api",
    "api:build": "npm run build --workspace=api",
    "auth:dev": "NODE_ENV=development npm run start:dev --workspace=auth",
    "auth:build": "npm run build --workspace=auth",
    "crypto-rates:dev": "NODE_ENV=development npm run start:dev --workspace=crypto-rates",
    "crypto-rates:build": "npm run build --workspace=crypto-rates",
    "posts:dev": "NODE_ENV=development npm run start:dev --workspace=posts",
    "posts:build": "npm run build --workspace=posts",
    "users:dev": "NODE_ENV=development npm run start:dev --workspace=users",
    "users:build": "npm run build --workspace=users",
    "db:dev:up": "docker-compose --env-file .env.development up db",
    "kafka:up": "docker-compose -f docker-compose.kafka.yml up",
    "kafka:down": "docker-compose -f docker-compose.kafka.yml down",
    "rmi": "docker rmi cryptotalks-api cryptotalks-db cryptotalks-crypto-rates cryptotalks-auth cryptotalks-front cryptotalks-posts cryptotalks-users",
    "dev:local": "export NODE_ENV=development && concurrently \"npm run dev --workspace=front\" \"npm run start:dev --workspace=api\" \"npm run start:dev --workspace=auth\" \"npm run start:dev --workspace=crypto-rates\"",
    "format": "npm run format --workspace=users && npm run format --workspace=posts && npm run format --workspace=api && npm run format --workspace=auth && npm run format --workspace=crypto-rates && npm run format --workspace=common",
    "prep": "npm --prefix . install --no-workspaces && npm --prefix services/api install --no-workspaces && npm --prefix services/auth install --no-workspaces && npm --prefix services/crypto-rates install --no-workspaces && npm --prefix services/front install --no-workspaces && npm --prefix services/posts install --no-workspaces && npm --prefix services/users install --no-workspaces",
    "demo": ""
  },
  "workspaces": [
    "./services/*"
  ],
  "keywords": [
    "cryptocurrency",
    "crypto",
    "bitcoin",
    "forum",
    "blog"
  ],
  "author": "Mikhail Bahdashych <contact@mikhailbahdashych.me>",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
