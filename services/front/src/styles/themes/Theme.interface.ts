export interface ThemeProps {
  colors: {
    primaryLight: string;
    primaryDark: string;
    lightBackground: string;
    darkBackground: string;
    textColor: string;
    svgColor: string;
  }
}
